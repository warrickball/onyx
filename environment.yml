name: onyx
channels:
  - defaults
dependencies:
  - pip
  - pip:
    - gunicorn
    - psycopg2-binary
    - django
    - djangorestframework
    - django-rest-knox
    - django-filter
    - django-cursor-pagination
    - celery
    - requests
    - django-query-tools
