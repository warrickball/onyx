name: metadb
channels:
  - defaults
dependencies:
  - pip
  - pip:
    - gunicorn
    - psycopg2-binary
    - django
    - djangorestframework
    - django-rest-knox
    - django-filter
    - django-model-utils
    - django-cursor-pagination
    - celery
    - requests
